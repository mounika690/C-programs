/*
WAP to count no.of digits in a number
*/

#include <stdio.h>
int main() 
{
  int num, count = 0;
  scanf("%d",&num);//reading number from user
  while(num != 0) 
  {
    num /= 10;
    count++;
  }
  printf("Number of digits: %d\n", count);
}
