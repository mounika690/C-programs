/*
WAP to rotate an array 'k' times
*/

#include<stdio.h>
  int main()
{
  int size;
  scanf("%d",&size);
  int arr[size];
  for(int i=0;i<size;i++)
  {
    scanf("%d",&arr[i]);
  }
  int last,k;
  scanf("%d",&k);
  for(int i=0;i<k;i++)
  {
    last=arr[size-1];
    for(int j=zise-1;j>0;j--)
    {
      arr[j] = arr[j-1];
    }
    arr[0] = last;
  }
  for(int i=0;i<size;i++)
  {
    printf("%d ",arr[i]);
  }
}
