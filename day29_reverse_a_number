/*
WAP to reverse a number
*/

#include <stdio.h>
int main()
{
  int num, rev = 0;
  scanf("%d",&num);//reading a number
  while(num != 0) 
  {
    rev = rev * 10 + num % 10;
    num =num / 10;
  }
  printf("Reversed number: %d\n", rev);
}
