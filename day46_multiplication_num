/*
WAF with no return type that prints multiplication table of a number
*/

#include<stdio.h>
  void mul(int num)
{
  for(int i=0;i<11;i++)
  {
    printf("%d * %d = %d\n",num,i,num*i);
  }
}
  int main()
{
  int num;
  scanf("%d",&num);
  mul(num);
}
